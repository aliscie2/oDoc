# pub struct RegisterUser {
#     pub name: Option<String>,
#     pub description: Option<String>,
#     pub photo: Option<Vec<u8>>,
#     pub email: Option<String>,
#     // pub keywords: Vec<String>,
# }
# create user backendCanister.register(affiliate_id: String, profile: RegisterUser)
# ger referal
# create contract
# deposit
# login as another user view referal id (affiliate_id)
# pub struct Affiliate {
#     pub id: String,
#     pub users: Vec<ReferredUser>,
#     pub earnings: Vec<ReferralPayments>,
#     pub stats: AffiliateStats,
# }
# get_affiliate_data(id: String) -> Result<Affiliate, String> {
# send frerequest to prev user
# sen payment to pref user
#  create job post
# create avaiablity caliendar 